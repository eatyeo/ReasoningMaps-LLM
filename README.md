1.  Make sure all files (`load_lsat.py`, `llm_client.py`, `requirements.txt`, `reasoning_parser.py`, `main.py`, `.env`) are in the same folder.
2.  Run `pip install -r requirements.txt`.
3.  Run the main application:
    ```bash
    python main.py